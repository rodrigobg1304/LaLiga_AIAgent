[project]
name = "football_agent"
version = "0.1.0"
description = "football_agent using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.9.3"
]

[project.scripts]
football_agent = "football_agent.main:run"
run_crew = "football_agent.main:run"
train = "football_agent.main:train"
replay = "football_agent.main:replay"
test = "football_agent.main:test"
run_with_trigger = "football_agent.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
